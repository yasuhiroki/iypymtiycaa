#!/usr/bin/env zsh

function zshef::install::osx() {
    zshef::util::mng::is_installed "herdr" || {
        zshef::util::os::mac::install "herdr"
    }
}


function zshef::update::osx() {
    zshef::util::mng::is_installed "herdr" && {
        zshef::util::os::mac::update "herdr"
    }
}
